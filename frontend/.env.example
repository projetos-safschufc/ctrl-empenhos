# =============================================================================
# Frontend – Controle de Empenhos e Estoque (Vite + React)
# Copie para .env ou .env.local e ajuste se necessário.
# =============================================================================

# URL base da API. Em dev com proxy do Vite, use /api (padrão).
# Para apontar para outro host: http://localhost:3001/api
# VITE_API_URL=/api
